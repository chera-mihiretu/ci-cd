global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "devops-demo-app"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:3000"]
        labels:
          environment: "demo"
